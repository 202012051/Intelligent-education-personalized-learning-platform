server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/iep_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}

jwt:
  secret: ${JWT_SECRET:iep-platform-secret-key-change-in-production-2026}
  expiration: 86400000  # 24 hours in ms

logging:
  level:
    com.iep: DEBUG
